{
  "name": "bodysense",
  "private": true,
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "dev:mobile": "turbo run dev --filter=@vitatekh/mobile",
    "dev:web": "turbo run dev --filter=@vitatekh/web"
  },
  "devDependencies": {
    "turbo": "^2.3.3",
    "typescript": "^5.7.2"
  },
  "packageManager": "pnpm@9.15.0",
  "engines": {
    "node": ">=20.0.0"
  },
  "pnpm": {
    "overrides": {
      "react-native-reanimated": "~3.16.1",
      "react-native-worklets": "^0.5.0"
    }
  }
}
